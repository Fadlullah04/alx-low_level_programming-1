#ifndef _3_FUNCTION_LIKE_MACRO_H_
#define _3_FUNCTION_LIKE_MACRO_H_

#define ABS(x) ((x) < 0 ? (-(x)) : (x))

#endif /* _3_FUNCTION_LIKE_MACRO_H_ */

//you can use _FILE_ instead of typing the actual filename. _FILE_ is a pre
//defined macro that detects the filename of the current working file
//it gets replaced with the actual filename 
